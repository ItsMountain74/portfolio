/**
 * Portfolio site configuration
 * Edit these values before deploying to GitHub Pages.
 */
window.PORTFOLIO_CONFIG = {
  /**
   * GitHub Pages base path.
   * - User/organization site (username.github.io repo): use ""
   * - Project site (username.github.io/repo-name): use "/repo-name"
   * Leave empty to auto-detect from the current URL.
   */
  basePath: "/ItsMountain.github.io",

  /** Change this password before going live. Default: admin123 */
  adminPassword: "1TapOneKill",

  /**
   * Optional: Formspree endpoint for contact form email delivery.
   * Example: "https://formspree.io/f/xxxxxxxx"
   * Leave empty to store messages locally only (export via admin dashboard).
   */
  formspreeEndpoint: "",

  siteName: "Alex Smith"
};
